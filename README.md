TOR project mod for ISPmanager

Run ./install_tor_mod.sh to install (under root)

Uses binary (compiled for every supported distro) of https://github.com/fallnet/shallot to produce custom TOR domain names
